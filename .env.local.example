# PostHog API Key
# Get your project key at https://us.posthog.com/project/settings
# Note: Host is configured via reverse proxy in next.config.ts
NEXT_PUBLIC_POSTHOG_KEY=phc_...

# Anthropic API key — powers the "Ask" feature (server-side only, never exposed
# to the client). Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...
